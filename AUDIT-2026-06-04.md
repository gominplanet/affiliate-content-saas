# Full-Codebase Audit — 2026-06-04 overnight

You asked: *"audit the entire site for speed, bugs and fix it all"*

I ran 5 parallel audits (security, performance, dead code, frontend, WP plugin), synthesized findings, and shipped fixes in batches. Below: what shipped, what didn't, and why.

---

## What shipped

_(populated as fixes land — agents are still returning at the time of writing)_

---

## What didn't ship + why

_(populated at end of run)_

---

## P0 / P1 / P2 findings — raw audit output

### WordPress plugin (PHP)

**P0 — Security / Data Loss**

| ID | File | Issue | Fix |
|---|---|---|---|
| W01 | mvpaffiliate-platform.php:254 | `mvp_affiliate_rest_save_customizations` writes raw `get_json_params()` into option — admin XSS surface for everything that later echoes from this option | Deep-sanitize array (whitelist known keys, `wp_kses_post`/`esc_url_raw` per value) before `update_option` |
| W02 | mvpaffiliate-platform.php:281 | `mvp_affiliate_render_block` echoes `$block['html']` raw on every post — stored XSS via W01 chain | `wp_kses_post($html)` |
| W03 | mvpaffiliate-platform.php:162 | IndexNow key endpoint missing `nocache_headers()` + uses `exit` not `wp_die` | Add headers + wp_die for proper teardown |
| W04 | mvpaffiliate-platform.php:1210,1240 | Unescaped `$bg` in inline `style` (CSS-context XSS) | `esc_attr($bg)` + validate against allowlist |
| W05 | mvpaffiliate-platform.php:1700 | Unescaped `$href` for mailto link — antispambot returns HTML entities, not URL-safe | `esc_attr` or `esc_url` |
| W07 | mvpaffiliate-platform.php:1747-1758 | `esc_attr` on color CSS doesn't strip `;` `}` `/* */` — breakout possible | Regex-validate `#[0-9a-f]{3,8}` before use |
| W08 | mvpaffiliate-platform.php:2365 | `/proxy` rate-limit is per-site only — attacker locks out legit traffic with 5 bad POSTs | Add per-IP counter + escalating cooldown |
| W09 | mvpaffiliate-platform.php:78 | `.htaccess` `file_put_contents` without `LOCK_EX` or backup | Add LOCK_EX + write `.htaccess.bak-mvp` first |
| W10 | mvpaffiliate-platform.php:2399 | `/proxy` accepts any REST path — leaked secret = full WP takeover via `/wp/v2/users` etc | Hard-allowlist routes (`/wp/v2/posts`, `/wp/v2/media`, `/affiliateos/v1/*`) |

**P1 — Performance**

| ID | File | Issue |
|---|---|---|
| W11 | :740,1013,646,663,680 | WP_Query in wp_footer + the_content with no transient caching (4-6 extra SQL/page) |
| W12 | :135 | `random_bytes(32)` will throw on stripped PHP — needs try/catch fallback to `wp_generate_password` |
| W13 | :1145-1156 | `pre_get_posts` forces posts_per_page=12 on all archives, overrides user setting |
| W14 | :1163 | `orderby => rand` on every page load — full-table sort on 500+ posts |
| W15 | :1768-1778 | `register_post_meta` in init — should be rest_api_init |
| W16 | :971-975 | `init` hook reads option every request to gate one-time flush |
| W17 | :212 | IndexNow submitted for staging / localhost domains |
| W18 | :1454 | AI Product Finder pill may render on wp-login.php (no is_login check) |
| W19 | mvp-affiliate-theme/inc/customizations.php:145 | `mt_srand` mutates global RNG state |
| W20 | :589-622 | Editors' Pick badge JS+CSS ships on every single-post even when no score |

**P2 — Polish** (W21–W30): json_encode→wp_json_encode, `..` segments in path regex, amazon-subdomain hijack heuristic, FAQ regex truncation if H2 in verdict, ad-hoc theme detection, etc.

---

_Reports from other agents (security, performance, dead-code, frontend) will append here as they return._
