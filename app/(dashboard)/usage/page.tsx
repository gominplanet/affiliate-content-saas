/**
 * /usage — Your usage: every limit on the plan, how much is used, and when it
 * resets. Open to every signed-in creator (components/usage/YourUsage).
 */
import YourUsage from '@/components/usage/YourUsage'

export default function UsagePage() {
  return <YourUsage />
}
